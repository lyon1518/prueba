function popSquema(type, typeClas, button, text, time) {
    this.type = type
    this.typeClas = typeClas
    this.button = button
    this.text = text
    this.time = time
}
export default popSquema