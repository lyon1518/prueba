function popSquema(type, typeClas, button, text, time, data) {
    this.type = type
    this.typeClas = typeClas
    this.button = button
    this.text = text
    this.time = time
    this.data = data
}
export default popSquema